return {
  "3rd/image.nvim",
  opts = {
    backend = "kitty", -- works with kitty/wezterm; set to "ueberzug" on X11
    max_width = 0,
    max_height = 0,
    max_width_window_percentage = 90,
    max_height_window_percentage = 90,
  },
}
