# ~/.config/nvim/.gitignore
# ─────────────────────────────
# Runtime & build artefacts
site/          # generated by :PackerInstall (legacy)
lazy-lock.json # <‑ Lazy.nvim lockfile (optional; keep if you want reproducible builds)

# Per‑machine state
.pyenvs/
.undo/
swap/
session/
shada/
spell/
backup/
log/
lazy-lock.json
# Remote‑plugin and Mason build caches
.local/
mason/

